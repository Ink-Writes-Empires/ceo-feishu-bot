# 🚀 飞书双向通信服务 · 一键部署指南

## 最快的方式：Railway免费部署（全程5分钟）

---

### 第一步：准备代码

把以下3个文件推到你自己的GitHub仓库：

```
飞书服务器/
├── app.py          （主程序 - 凭证已内置）
├── requirements.txt（依赖）
└── Procfile        （启动配置）
```

或者直接用下面的"复制粘贴部署"方式。

---

### 第二步：部署到Railway（免费 · 推荐）

1. 打开 https://railway.app  → GitHub登录
2. 点击「New Project」→「Deploy from GitHub repo」
3. 选择你的代码仓库
4. 自动检测到Python → 自动部署 ✅
5. 部署成功后，拿到你的URL：`https://xxxxx.railway.app`

---

### 第三步：配置飞书应用

回到飞书开放平台 → 你的应用「CEO决策机器人」：

#### 3.1 配置事件订阅URL
```
左侧菜单 → 事件订阅 → 请求地址URL
填写：https://你的域名.railway.app/webhook/feishu
点击「验证」→ 通过 ✅
```

#### 3.2 添加订阅事件
```
点击「添加事件」→ 添加：
- im.message.receive_v1（接收单聊消息）
- im.message.group_v1（接收群聊消息）
```

#### 3.3 添加权限
```
左侧菜单 → 权限管理 → 添加：
- im:chat:readonly
- im:group:readonly
- im:message:send
- im:message.group_msg
- im:message:read
- im:message.group_at_msg
```

#### 3.4 发布应用
```
左侧菜单 → 版本管理 → 创建版本
版本号: 1.0.0
发布方式: 企业内
点击「申请发布」
```

---

### 第四步：把机器人加到群里

打开飞书App → 进入「跨境电商AI系统」群聊：

```
群设置 → 群机器人 → 添加机器人
→ 选择「CEO决策机器人」
→ 确认添加 ✅
```

---

### 第五步：测试

在群里@CEO机器人：

```
@CEO机器人 帮我调研蓝牙耳机
```

应该立刻收到回复：
```
🤖 CEO收到，正在处理中...
```

然后几十秒后收到完整回复。

---

## 备用方案：Render部署

如果Railway不行，用Render也一样：

1. 打开 https://render.com  → GitHub登录
2. 点击「New +」→「Web Service」
3. 连接你的GitHub仓库
4. 设置：
   - Name: `ceo-feishu-bot`
   - Environment: `Python 3`
   - Build Command: `pip install -r requirements.txt`
   - Start Command: `gunicorn app:app --bind 0.0.0.0:$PORT`
5. 选择 Free 套餐 → 部署 ✅

---

## 调试方法

部署后访问以下地址检查状态：

```
https://你的域名.railway.app/
→ 返回 {"status": "running", ...}  ✅

https://你的域名.railway.app/api/status
→ 返回飞书Token状态和消息队列
```

---

## ⚠️ 重要提示

1. 第一次部署后需要**几分钟**等待启动
2. 免费服务如果15分钟没有请求会休眠，唤醒需要几秒
3. App Secret已内置在代码中，如果代码公开到GitHub，建议改用环境变量

---

**部署完成了吗？把部署后的域名发给我，我来帮你配置飞书应用的事件订阅！** 🚀
